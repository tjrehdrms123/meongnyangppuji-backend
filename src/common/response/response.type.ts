import { ApiCreatedResponseTypeInterface } from "./base/response.type.interface";

// 200: 새로운 리소스를 성공적으로 생성한 경우
export const ApiCreatedResponseType: ApiCreatedResponseTypeInterface = {
    "success": true,
    "data": {}
}